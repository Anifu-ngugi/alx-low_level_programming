#include "main.h"
/**
 * mul- a program that multiplies two numbers
 * @a: first parameter to be computered
 * @b: second parameter to be computed
 * Return: integer of the multiplied value
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);

}
